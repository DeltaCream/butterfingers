# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Environment configuration files which will inevitably differ between development environments (namely, the host Windows system, and the emulated Ubuntu system)
.env

# Visual Studio Code settings configuration via settings.json, for the same reasons as above (on *.env)
.vscode/settings.json

# MySql session files
*.session.sql

#pyfi_db.sql
pyfi_db.sql